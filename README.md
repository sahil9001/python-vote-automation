### **Welcome to the python-vote-automation wiki!**


A simple script to automate the yes/no poll.

**Setup**:

* Git clone the repo.
* Replace variables in `bot1.py` with the desired links.
* Run `./bot.sh` on your terminal.
